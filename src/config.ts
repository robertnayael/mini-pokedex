/** 
 * How many cards are retrieved for display in the main list at start
 * and then every time the user scrolls down to the end of the list
 * */
export const cardBatchSize = 20;

/** Displayed in the top header and the document title */
export const appTitle = 'NeoPokedex';
